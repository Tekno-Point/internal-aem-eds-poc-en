const forms = [];

const tabsForm = document.querySelector('trip-form-modal');

forms.push(tabsForm);

function formToggler(formId) {
  const isSingleMultiTrip = formId === 'single-trip' || formId === 'single-trip';

  if (isSingleMultiTrip) {
    if (isSingleMultiTrip == 'single-trip') {
      //toogle single-trip
    } else {
      //toogle multi-trip 
    }
  }

}

